using UnityEngine;

namespace ProjectATLAS
{
    public class GemricarScript : MonoBehaviour
    {
        [SerializeField] private string yourName;
        [SerializeField] private string yourAge;
        [SerializeField] private string yourSchool;
        
        
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
